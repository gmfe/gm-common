import { instance, Request } from './request'

// 默认配置好 headers
import configHeaders from './config_headers'
import configTrace from './config_trace'

export { instance, Request, configHeaders, configTrace }
